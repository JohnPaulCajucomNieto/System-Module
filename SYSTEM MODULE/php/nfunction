<?php 

    require_once 'conect.php';

    function display(){
        global $con;
        $query = "SELECT * FROM storages";
        $result = mysqli_query($con,$query);
        return $result;
    }
?>